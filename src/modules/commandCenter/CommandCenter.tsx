// Placeholder — Command Center's actual functionality (property-scoped
// email management) arrives with Phase 3 (3.1-3.4). Nav entry added by
// roadmap 10.1; nothing to build here yet beyond stating that plainly.
export function CommandCenter() {
  return (
    <div>
      <h1>Command center</h1>
      <p>Command Center is coming with Phase 3's email/Drive integration.</p>
    </div>
  )
}
